# Lambda Function

This folder contains the Python source code used in the AWS Lambda function.

Files include:

- lambda_function.py

- requirements.txt

- sample_event.json

The function processes CSV files stored in Amazon S3 and generates aggregated output datasets.